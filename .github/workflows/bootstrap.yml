name: Bootstrap project

on:
  push:
    branches:
      - agent/initial-sysdiff
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  bootstrap:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout head branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          fetch-depth: 0

      - name: Expand project archive
        shell: bash
        run: |
          cat .bootstrap/parts/part-* > /tmp/project.tar.gz
          find . -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} +
          tar -xzf /tmp/project.tar.gz

          # GITHUB_TOKEN cannot create workflow files. Keep this workflow
          # byte-for-byte unchanged; final CI workflows are added by GitHub App.
          rm -rf .github/workflows
          mkdir -p .github/workflows
          git show HEAD:.github/workflows/bootstrap.yml > .github/workflows/bootstrap.yml

      - name: Commit project
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Создать SysDiff MVP" || exit 0
          git push origin HEAD:${{ github.head_ref || github.ref_name }}
